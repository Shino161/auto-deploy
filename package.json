{
  "name": "auto-deploy",
  "version": "1.0.0",
  "description": "code auto deploy",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "iceOolongTea",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^0.7.1"
  }
}
